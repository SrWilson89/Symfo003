<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ empresa }} - Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .card-body {
            text-align: center;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_dashboard') }}">Mi Piscina</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_dashboard' %}active{% endif %}" aria-current="page" href="{{ path('app_dashboard') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_tpv' %}active{% endif %}" href="{{ path('app_tpv') }}">TPV</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_arqueo' %}active{% endif %}" href="{{ path('app_arqueo') }}">Arqueo</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="listadosDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Listados
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="listadosDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_list', {'entityName': 'clientes'}) }}">Clientes</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_list', {'entityName': 'empleados'}) }}">Empleados</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_list', {'entityName': 'productos'}) }}">Productos</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_list', {'entityName': 'ventas'}) }}">Ventas</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_list', {'entityName': 'asociados'}) }}">Asociados</a></li>
                        </ul>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Salir</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Dashboard</h1>

        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card text-white bg-primary mb-3">
                    <div class="card-header">Total de Ventas de Hoy</div>
                    <div class="card-body">
                        <h5 class="card-title display-4">{{ totalVentas|number_format(2, ',', '.') }} €</h5>
                    </div>
                </div>
            </div>
        </div>
        
        <h2 class="mt-5">Ventas por Hora</h2>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-body">
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Hora</th>
                                    <th>Total de Ventas</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for venta in ventasPorHora %}
                                    <tr>
                                        <td>{{ venta.hora }}</td>
                                        <td>{{ venta.total|number_format(2, ',', '.') }} €</td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="2">No hay ventas registradas para hoy.</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
